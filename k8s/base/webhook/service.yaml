apiVersion: v1
kind: Service
metadata:
  name: aios-webhook
  namespace: aios
  labels:
    app.kubernetes.io/name: aios-webhook
    app.kubernetes.io/part-of: aios
spec:
  selector:
    app.kubernetes.io/name: aios-webhook
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
