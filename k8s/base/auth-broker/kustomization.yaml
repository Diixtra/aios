apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aios

resources:
  - serviceaccount.yaml
  - clusterrole-tokenreview.yaml
  - pvc.yaml
  - onepassword.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml

commonLabels:
  app.kubernetes.io/name: auth-broker
  app.kubernetes.io/component: auth
