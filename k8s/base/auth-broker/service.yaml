apiVersion: v1
kind: Service
metadata:
  name: auth-broker
  namespace: aios
spec:
  type: ClusterIP
  selector:
    app: auth-broker
  ports:
    - name: http
      port: 8080
      targetPort: http
