apiVersion: v1
kind: Service
metadata:
  name: aios-voice
  namespace: aios
  labels:
    app.kubernetes.io/name: aios-voice
    app.kubernetes.io/part-of: aios
spec:
  selector:
    app.kubernetes.io/name: aios-voice
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
