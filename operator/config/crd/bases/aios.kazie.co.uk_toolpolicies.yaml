---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: toolpolicies.aios.kazie.co.uk
spec:
  group: aios.kazie.co.uk
  names:
    kind: ToolPolicy
    listKind: ToolPolicyList
    plural: toolpolicies
    singular: toolpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ToolPolicy is the Schema for the toolpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowed:
                properties:
                  commands:
                    items:
                      type: string
                    type: array
                  fileAccess:
                    properties:
                      readable:
                        items:
                          type: string
                        type: array
                      writable:
                        items:
                          type: string
                        type: array
                    type: object
                  network:
                    properties:
                      allowedHosts:
                        items:
                          type: string
                        type: array
                    required:
                    - allowedHosts
                    type: object
                required:
                - commands
                type: object
              denied:
                properties:
                  commands:
                    items:
                      type: string
                    type: array
                  fileAccess:
                    items:
                      type: string
                    type: array
                type: object
            required:
            - allowed
            type: object
        type: object
    served: true
    storage: true
