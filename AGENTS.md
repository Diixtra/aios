# Agent Instructions

This repo follows the diixtra-coding-guidelines skill (`~/.claude/skills/diixtra-coding-guidelines`).

## Guideline Overrides

None at this time.

## Code-Quality CI

This repo uses the org-wide reusable code-quality workflow at `Diixtra/diixtra-forge/.github/workflows/code-quality.yaml@main`. The caller is `.github/workflows/code-quality.yaml`. Currently in `mode: observe` — findings are advisory and do not gate merge. See [diixtra-forge/docs/code-quality/rollout.md](https://github.com/Diixtra/diixtra-forge/blob/main/docs/code-quality/rollout.md) for the rollout SOP.
